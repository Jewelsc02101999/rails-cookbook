<h1> Add recipe to category<%=@category.name%> category</h1>

<%= simple_form_for [@category, @bookmark] do |f| %>
  <%= f.input :comment %>
   <%= f.association :recipe %>
  <%= f.submit :submit %>
<% end %>
